<!-- views/index.ejs -->

<!DOCTYPE html>
<html>
  <body>

    <div class="jumbotron">
      <h1>Purde Lab Stats</h1>

        <p>Welcome</p>

       <% console.log(debug); %>
       <% if (user != undefined) { %>
          <p><%= user %></p>
       <% } else {%>
          <p>not logged in</p>
          <%= signInUrl %>
       <% } %>
       
        <a class="btn btn-primary btn-large" href="<%= signInUrl %>">Click here to login</a>
     
    </div>

  </body>
</html>
